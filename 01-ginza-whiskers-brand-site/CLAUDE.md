# GINZA WHISKERS ブランドサイト Project 2026 開発憲章（Project 01）

> **このファイルの位置づけ**：本ファイルは Project 01（ブランドサイト）専用の
> 開発憲章である。ワークスペース全体（複数プロジェクト横断）の理念・PMO運用
> ルール・命名規則・ドキュメント基準・品質基準は、リポジトリルートの
> `CLAUDE.md`（Root憲章）を参照すること。
>
> 本ファイルは、2026-07-19に単一プロジェクトの開発憲章として策定された旧
> `CLAUDE.md`（リポジトリルート）の内容を、2026-07-20のワークスペース構造
> 再編にあたって**一切の削除・簡略化を行わずそのまま移設**したものである。
> Root憲章と重複する記述（ブランド理念の一部、PMO運用の一般原則など）が
> あるが、既存の実装ノート・チェックリスト・設計判断を一つも失わないことを
> 優先し、意図的に残してある。今後の更新は本ファイル側（Project 01固有の
> 内容）に対して行う。

このファイルは Claude Code（claude.ai/code）がこのリポジトリで作業する際の指針であり、
同時に本プロジェクトの「開発憲章」でもある。単なる技術メモではなく、プロジェクトの目的・理念・
方針・優先順位・運用ルールを一つの場所にまとめ、数か月にわたる開発を通じて一貫性を保つための
基準文書として機能させる。

このファイルの記述はデフォルトの振る舞いに優先する。内容は固定ではなく、意思決定が起きるたびに
更新していくべき「生きた文書」である。特に第10章「現在のステータス」は毎回のセッションで参照・
更新すること。

---

## 1. プロジェクトの目的

銀座昭和浪漫倶楽部（GINZA WHISKERS）は、銀座という街が昭和という時代に育んだ「浪漫」——
文化・芸術・建築・人物・出来事の記憶——を掘り起こし、言葉とかたちに残し、国内外へ発信する
文化メディアプロジェクトである。

過去を懐古するだけでなく、**過去・現在・未来の銀座へ「提言」すること**が目的である。
単なるアーカイブサイトではなく、銀座の価値を再定義し、次の世代・海外へ手渡すためのメディアと
位置づける。

## 2. ブランド理念

- **昭和浪漫**：ネオンと喧騒の裏側にある文化の厚み、人々の矜持、建築に刻まれた意匠。放って
  おけば静かに失われていくものを記録し、言葉にする。
- **6つの視点（Six Pillars）**：歴史・文化・アート・建築・人物・イベント。すべてのコンテンツは
  この6本柱のいずれかに接続することを意識する。
- **トーン**：上品で落ち着いた文体。過度に商業的・扇動的な表現は避け、取材と記録に基づく
  信頼できる語り口を保つ。
- **視座のスケール**：銀座という一つの街区から、日本の近代文化、そして世界への発信を志向する。
  ローカルであることが、そのままグローバルな価値になるという逆説を軸に据える。

## 3. 10月公開目標

- **目標公開日**：2026年10月（年内で最も優先度の高いマイルストーン）。
- 公開時点で目指す状態は「完璧なアーカイブサイト」ではなく、**ブランドの理念と信頼性が
  正しく伝わる最小限の完成形**。コンテンツの厚みは公開後も継続的に拡充していく前提とする。
- 公開の可否判断は第9章「公開前チェックリスト」を基準とする。
- **10月公開は全プロジェクトの最優先目標であり、すべての提案・作業判断はこの目標に
  近づくかどうかを基準に行う**（優先順位の考え方は第7章、運用上の徹底は8.2）。

## 4. 開発方針

- **実装よりブランド設計を優先する（原則。詳細は8.3）。** コーディングに着手する前に、
  必ずブランド設計・仕様面をユーザーと確認し、合意を得る。「まず作ってから見せる」のでは
  なく「設計を確認してから作る」を徹底する。
- **ビルドツール・フレームワークなしの静的サイトを維持する。** 現在の規模・更新頻度において
  ビルドチェーンを導入する必然性はない。将来、記事アーカイブの規模が大きくなり管理コストが
  無視できなくなった時点で初めて再検討する（先回りした技術選定はしない）。
- **共有マークアップ（ヘッダー・フッター）は手動同期。** テンプレート機構がないため、
  `index.html` と `about.html` の両方を必ず同時に編集する。ページが3つ以上に増える段階で
  静的インクルードの導入を再検討する。
- **依存を増やさない。** 外部サービスへの依存（Google Fonts、note.com など）は現状維持しつつ、
  新規追加時は「なぜ自前で持たないのか」を一言で説明できることを条件とする。
- **シンプルさを優先する。** 一つのタスクのために抽象化・設定オプション・将来のための
  拡張性を作り込まない。3行の重複は、早すぎる共通化より良いことが多い。

## 5. デザイン方針

- 配色・タイポグラフィは `css/style.css` の `:root` カスタムプロパティで一元管理する
  （近黒背景・オフホワイト文字・ゴールドアクセント／見出し: Noto Serif JP・Cormorant
  Garamond、本文: Noto Sans JP）。個別ルールでの上書きではなく変数側を変更する。
- トーンは「昭和浪漫」の温度感——過度に近未来的・カジュアルにならないこと。
- レスポンシブ対応必須（現状ハンバーガーメニューで対応済み）。
- アクセシビリティは最低限（aria属性など）を維持しつつ、画像追加時には alt テキストの
  質を軽視しない。デザインの美しさと可読性・操作性はどちらかを犠牲にしない。

## 6. コンテンツ方針

- コンテンツは常に6本柱（歴史・文化・アート・建築・人物・イベント）のいずれかに紐づける。
- 記事本体は当面 note.com での連載を軸とし、本サイトはブランドの「顔」として機能する
  （note連載からサイト内アーカイブへの統合は中長期の検討事項——第9章参照）。
- 多言語対応（英語版）は理念上は視野に入っているが、着手時期・範囲は **未定**。決定次第
  この章に追記する。
- 実写真・グラフィックが未着手（`images/` は空）。素材が入り次第、命名規則・最適化方針を
  この章に追記する。

## 7. 優先順位の考え方

作業の優先度は以下の順で判断する。上位が未達のまま下位に着手しない。

1. **公開ブロッカー**：これが欠けていると公開できないもの（TODOプレースホルダーの解消、
   実素材の最低限の確保など）。
2. **信頼性・信用**：訪問者が「実在する、信頼できる媒体」と認識できること（運営者情報、
   実在する連絡手段、法的に必要な表示）。
3. **発見可能性**：検索・SNSでの見え方（OGP、favicon、sitemap、robots.txt）。
4. **コンテンツの厚み**：6本柱それぞれの記事・素材の拡充。
5. **付加的な最適化**：パフォーマンス、多言語化、高度なSEO施策など、公開後でも致命的でない改善。

迷った場合は「これが欠けたまま10月に公開して致命的か」を判断基準にする。

## 8. Daily PMOの運用

このプロジェクトには専任のPMツールがない。そのため、Claude Codeとのセッションそのものを
軽量なPMO（プロジェクトマネジメントオフィス）として機能させる。運用は以下の5ルールに従う。

### 8.1 セッション開始時の「Daily PMO」表示（必須）

新しいセッションで作業に入る前に、必ず以下の形式で「Daily PMO」を提示する。省略しない。

```
【Daily PMO】YYYY-MM-DD
- 全体進捗率: ●●%（算出根拠を一言）
- 前回からの進捗: ...
- 今日のゴール: ...
- 今日の最優先タスク（最大3件）:
  1. ...
  2. ...
  3. ...
- リスク: ...
- 次回への引き継ぎ: ...
```

- **全体進捗率**は、第9章チェックリストのうち「判断保留事項」を除く項目（必須／信頼性／
  発見可能性／インフラ）の完了数 ÷ 総数で機械的に算出し、算出根拠を一言添える。
- 「前回からの進捗」「次回への引き継ぎ」は第10章「現在のステータス」を参照して作成する。
- Daily PMOは現状把握のための表示であり、これ自体が目的化しないよう簡潔に保つ。

### 8.2 公開最優先の原則

10月公開が全プロジェクトの最優先目標である（第3章）。すべての提案・タスク選定は
「これは公開に近づくか」を判断基準とする（優先順位の考え方は第7章）。公開に直接
寄与しない作業を提案する場合は、なぜ今それを行う価値があるかを明示する。

### 8.3 ブランド設計優先の原則

本サイトはブランドサイトである。実装（コーディング）よりも先にブランド設計・仕様の
すり合わせを優先する。ユーザーとの設計合意が取れていない状態で実装に着手してはならない。
「まず作ってから見せる」ではなく「設計を確認してから作る」を徹底する（第4章参照）。

### 8.4 セッション終了時の提案（必須）

状況のレビュー・分析だけで会話を終えてはならない。毎回、「今日どこまで進めるべきか」
という具体的な次の一手を提案して締めくくる。

### 8.5 意思決定の記録

方針転換・仕様確定・優先順位の変更など、後から「なぜそうしたか」が分からなくなる
意思決定は、その場で第10章に一行で記録する（詳細な経緯は会話ログやコミットメッセージに
委ねてよいが、結論だけはここに残す）。ここに書くのは意思決定の結論のみ。実装の詳細や
タスクの逐次進捗は書かない（肥大化を防ぐため）。

## 9. 公開前チェックリスト

10月公開に向けて、以下がすべて解消されている状態を「公開可能」の基準とする。
（2026-07-19時点のレビューに基づく初版。状態が変わり次第、印を更新する。）

**必須（公開ブロッカー）**
- [x] `index.html` の note.com URL を実際のアカウントURLに差し替え（2026-07-26）
- [x] `index.html` の連絡先 `mailto:` を実際のアドレスに差し替え（2026-07-26）
- [ ] 実写真・グラフィック素材を `images/` に配置——**スコープ確定済み
      （2026-07-26）：最低限ヒーロー画像1枚のみ**。実素材は取材・取得が
      これから必要なため未着手（実装仕様は第10章・付録Bを参照、素材が
      揃い次第すぐ実装できる状態にしてある）
- [x] 運営者情報の掲載方針を決定・反映（2026-07-26。詳細は第10章参照）

**信頼性**
- [x] `mailto:` 直書きによるスクレイピング対策（フォーム化 or 難読化）を検討（2026-07-28。JS難読化を採用、第10章参照）
- [x] プライバシーポリシー等の要否を判断・実装（2026-07-28。`privacy.html`
      として実装済み。第10章参照）

**発見可能性**
- [x] favicon / apple-touch-icon（2026-07-26）
- [x] OGP / Twitter Card メタタグ（2026-07-26。og:url/og:imageは独自ドメイン
      未確定のためginzawhiskers.com想定の仮実装、第10章参照）
- [x] `robots.txt` / `sitemap.xml`（2026-07-27。og:url/og:imageと同じく
      ginzawhiskers.com想定の仮実装、第10章参照）
- [x] canonical URL（2026-07-27。同上、仮ドメイン）
- [x] 404ページ（2026-07-28。第10章参照）

**インフラ**
- [ ] 独自ドメイン（`ginzawhiskers.com`で確定）とホスティング先
      （GitHub Pagesで確定）の実際の設定——**決定は完了（2026-07-28）**、
      ドメイン取得・DNS設定・GitHub Pages有効化は未着手。手順は付録D参照
- [x] アクセス解析（GA4等）導入方針の決定——**方針決定は完了
      （2026-07-28）**、詳細はリポジトリルート`CLAUDE.md`第9章参照
      （01・02・Bar LIBRE横断の方針のためRoot側に一本化）。GA4タグの
      実際の実装は10月公開直前に行う（未着手）

**判断保留事項**
- [ ] 記事アーカイブをサイト内に持つか、note.com依存を続けるか
- [ ] 英語版（多言語対応）の要否・時期

## 10. 現在のステータス

*このセクションは第8章のルールに従い、セッションごとに更新する。*

- **フェーズ**：コンテンツ・体制準備フェーズ（開発憲章 v1 策定直後）
- **目標公開日**：2026年10月
- **直近の意思決定**：
  - 2026-07-19: CLAUDE.mdを技術メモから「開発憲章」形式へ全面改訂。プロジェクトの目的・
    理念・優先順位・Daily PMO運用ルール・公開前チェックリストを制定。
  - 2026-07-19: Daily PMOの運用を具体化。①セッション開始時のDaily PMO表示（進捗率・
    前回からの進捗・今日のゴール・最優先タスク3件・リスク・引き継ぎ）を必須化、②公開
    最優先の原則、③ブランド設計優先（実装前に設計確認必須）、④セッション終了時に
    「今日どこまで進めるべきか」の提案を必須化。
  - 2026-07-20: ワークスペース構造再編に伴い、本ファイル（旧リポジトリルート
    CLAUDE.md）をProject 01専用の開発憲章として `01-ginza-whiskers-brand-site/
    CLAUDE.md` に移設。内容の削除・簡略化は行わず、全文をそのまま引き継いだ。
    ワークスペース全体の理念・PMO運用ルール・命名規則等はリポジトリルートの
    新しい `CLAUDE.md`（Root憲章）に分離。
  - 2026-07-26: 公開ブロッカーのうちnote.com URL・連絡先メールアドレスを確定した。
    note.com URLは既存アカウント`https://note.com/ginza_whiskers`（ページタイトル
    「GINZA WHISKERS｜note」であることをfetchして確認済み）。連絡先メールは
    `ginzashowaromanticclub@gmail.com`（独自ドメイン未取得のため、当面Gmail
    アドレスを使用する方針）。`index.html`の該当TODOコメント2箇所を解消し、
    第9章チェックリストの該当2項目にチェックを入れた。`mailto:`の
    スクレイピング対策（信頼性チェックリスト）は今回のスコープ外で未着手のまま。
  - 2026-07-26: 運営者情報の掲載方針を確定した。銀座昭和浪漫倶楽部は法人格を持たない
    任意団体（有志の集まり）であり、サイト上で直接的な決済・有料サービス提供の予定も
    ないため、特定商取引法等の法定表示義務は発生しないことを確認した。信頼性確保の
    観点からは**最小限の開示（団体名のみ。代表者個人名・住所は非公開）**を方針とし、
    既存の表示（`index.html`/`about.html`のヘッダーブランド表記・フッター著作権表記
    「銀座昭和浪漫倶楽部 GINZA WHISKERS」、および直前に確定した連絡先メール）で
    この方針は既に満たされていると判断した。**コード変更は不要**——今回はサイト内容の
    追加ではなく方針決定と充足確認のみ。
  - 2026-07-26: 実写真・グラフィック素材のスコープを確定した。現状index.html/
    about.htmlとも`<img>`・`background-image`を一切使わないテキスト・
    タイポグラフィ中心の構成であることを確認したうえで、公開に必要な最低限を
    **ヒーロー画像1枚のみ**とすることに決定（6本柱カードへの画像追加は
    見送り、テキストのみで継続）。実素材は取材・取得がこれから必要な段階で
    現時点では供給できないため、**この意思決定はスコープ確定のみであり
    未実装のまま**——AIによる代替画像の生成は行わない（実在の昭和期銀座を
    記録するアーカイブという本サイトの信頼性の前提と矛盾するため）。
    素材が届き次第すぐ実装できるよう、実装仕様を付録Bに追記した。
  - 2026-07-26: favicon・OGP/Twitter Cardメタタグを実装した。**favicon**：
    「銀」の一文字（ゴールド`#c9a45c`、近黒`#0b0b0c`背景、ヒラギノ明朝 ProN）を
    モチーフとし、`favicon.svg`（ベクター、主要ブラウザ向け）・`favicon.ico`
    （16/32/48pxマルチ解像度）・`apple-touch-icon.png`（180×180）をリポジトリ
    ルートに配置（favicon類はブラウザの規約上サイトルート直下が標準のため
    `images/`ではなくルートに置いた）。**OGPカード**：`images/og-card.jpg`
    （1200×630、団体名＋「GINZA WHISKERS」＋タグライン、ブランド配色で新規
    作成。実写真は使わず、ブランドの信頼性の前提と矛盾しない）。og:title/
    og:description は各ページの既存`<title>`/`meta description`と一致させた。
    **既知の制約**：og:url・og:imageは仕様上絶対URLが必須のため、独自ドメイン
    未確定の現時点では想定ドメイン`https://ginzawhiskers.com/`で仮実装した
    （HTML内にコメントで明記。Project 02のPhase 7で同種の判断をした前例に
    倣った）。ドメイン確定時に実際の値へ更新が必要（付録Cに記録）。**検証**：
    ローカル静的サーバーで`favicon.ico`/`apple-touch-icon.png`/
    `images/og-card.jpg`が200で配信されることを確認。ブラウザ拡張
    （claude-in-chrome）は本セッションで未接続のため、実ブラウザでのタブ
    アイコン表示の目視確認とSNSクローラーによる実際のOGP展開確認は未実施
    のまま残る。
  - 2026-07-27: `robots.txt`・`sitemap.xml`・canonicalタグを実装した。
    `robots.txt`は全クローラーへのクロール許可と`sitemap.xml`の場所を明示。
    `sitemap.xml`は現存する2ページ（`index.html`, `about.html`）を登録
    （トップページ優先度1.0、aboutを0.8）。canonicalタグは各ページの
    `<head>`に`<link rel="canonical">`として追加し、`<meta name="description">`
    の直後に配置。**ドメインについて**：canonical URL・sitemap内のURLは
    仕様上絶対URLが必須のため、og:url/og:imageと同じく独自ドメイン未確定の
    現時点では想定ドメイン`https://ginzawhiskers.com/`で仮実装した
    （2026-07-26のOGP実装時の前例を踏襲）。ドメイン確定時に
    `robots.txt`・`sitemap.xml`・両HTMLファイルのcanonical値をまとめて
    実際の値へ更新する必要がある（付録C参照）。**検証**：ローカル静的
    サーバーで`robots.txt`/`sitemap.xml`/`index.html`/`about.html`が
    いずれも200で配信されることを確認済み。
  - 2026-07-28: 404ページ（`404.html`）を新規実装した。既存ページ
    （`index.html`/`about.html`）と共通のヘッダー・フッター・フォント・
    配色を踏襲し、`css/style.css`に`.error-code`クラス（大きな「404」を
    ゴールドのセリフイタリック体で表示）を追加。本文では簡潔な案内文と
    トップページへ戻る`.btn`ボタン（`index.html`へのリンク）を設置。
    **SEO**：`<meta name="robots" content="noindex, nofollow">`を付与し、
    `sitemap.xml`には含めない方針とした（404ページ自体が検索結果に出る
    ことを防ぐため）。ファイル名`404.html`はGitHub Pages・Netlifyなど
    主要な静的ホスティングサービスが慣習的に自動認識するファイル名だが、
    実際の自動ルーティングはホスティング先確定後（第9章「インフラ」参照）
    に改めて確認が必要。**検証**：ローカル静的サーバーで`404.html`/
    `index.html`/`css/style.css`が200で配信されることを確認済み。ブラウザ
    拡張（claude-in-chrome）は本セッションで未接続のため、実ブラウザでの
    表示の目視確認は未実施のまま残る（2026-07-26のfavicon実装時と同じ制約）。
  - 2026-07-28: `mailto:`スクレイピング対策として、JS難読化を採用した
    （フォーム化案は不採用——外部フォームサービス連携が必要になり静的サイト
    構成から外れること、プライバシーポリシー要否の判断に波及することから、
    ユーザーがより影響範囲の小さいJS難読化を選択）。`index.html`の
    `mailto:`リンクを`<a id="contact-email" class="btn"></a>`（空）に変更し、
    `js/main.js`でユーザー名部分・ドメイン部分を別々の文字列として保持した
    うえで実行時に結合し`href`とテキストを組み立てる方式にした。これにより
    `index.html`のHTMLソース、`js/main.js`の文字列検索のいずれにも
    メールアドレス全体が平文で出現しなくなる（単純なHTML走査型スクレイパー
    への対策。JavaScriptを実行するクローラーへの完全な防御ではない）。
    JS無効環境向けに`<noscript>`内へ元のmailtoリンクをフォールバックとして
    残した（JS有効時は`<noscript>`内容はブラウザにより非表示になるため
    通常は露出しない）。**検証**：ローカル静的サーバーで`index.html`/
    `js/main.js`が200で配信されること、`js/main.js`内にメールアドレス
    全体の文字列が存在しないことを確認済み。
  - 2026-07-28: プライバシーポリシーの要否を判断した。現状分析：フォーム・
    アクセス解析は未導入で、サイトが能動的に個人情報を収集する仕組みは
    ない（問い合わせは`mailto:`のみ）。Google Fontsを自己ホストせずCDN
    経由で読み込んでいるため訪問者IPアドレスがGoogleへ送信される実際の
    データフローは存在するが、日本の個人情報保護法上IPアドレス単体は
    原則「個人情報」に該当せず国内法的リスクは低いと判断。特定商取引法は
    既に対象外と確認済み（2026-07-26）。**結論**：法的必要性は現時点で
    低いが、GA4導入（第9章「インフラ」、未決定）や将来的なフォーム化を
    行う場合はcookie・解析ツールの利用目的開示が事実上必須になるため、
    設置自体は将来的に推奨。記載内容（特にcookie条項）はGA4導入可否に
    左右されるため、**GA4導入方針が確定してから実装する**方針を決定した。
    公開ブロッカーには該当しないため、10月公開のスケジュールには影響
    しない。**今回はコード変更なし**——方針決定のみ。
  - 2026-07-28: ドメイン/ホスティングの設計方針を決定した。**ホスティング：
    GitHub Pages**（既にコードが`github.com/maron-ginza/GINZA_WHISKERS`に
    push済みで、新規アカウント作成・外部サービス連携が不要なため。
    ビルドツールを使わない現在の静的サイト構成とも合致し、`404.html`も
    ホスティング側が自動認識する）。Netlify・Cloudflare Pages・Vercelも
    比較検討したが、いずれも新規アカウント管理コストが増える割に今回の
    規模では機能過多と判断し不採用。**ドメイン：`ginzawhiskers.com`で
    確定**（OGP/canonical/sitemap/robots.txtで既に仮実装していた想定
    ドメインをそのまま正式採用。これにより付録Cの「暫定値」は今後
    ドメイン変更がない限り更新不要になった）。実際の設定手順は付録Dに
    ランブックとして記録した。**今回はドメイン取得・DNS設定・GitHub Pages
    の有効化は未実施**——決定のみで、実際の取得・設定は次のアクション。
  - 2026-07-28: アクセス解析（GA4）導入方針を決定した。01・02・Bar LIBRE
    （構想段階、未着手）に横断する方針のため、詳細はリポジトリルート
    `CLAUDE.md`第9章に一本化して記録した。要点のみ：①GA4タグの実装は
    10月公開直前に行う（方針決定と実装を分離）、②これによりプライバシー
    ポリシー実装（同日決定分）が着手可能になった、③Google Signals等の
    広告機能はオフを既定とし、現状はCookie同意バナー不要と判断、④GA4は
    サイト（事業）単位でプロパティを分ける構成とする。**今回はコード
    変更なし**——方針決定のみ。
  - 2026-07-28: プライバシーポリシーを実装した（`privacy.html`）。GA4方針
    決定により内容が確定していたため実装に着手。構成：①お問い合わせ
    （`mailto:`）で取得する情報と利用目的、②アクセス解析ツール（GA4、
    導入予定）についてGoogle Signalsを有効化しない旨とオプトアウト方法、
    ③Google Fontsの利用によるIPアドレス送信について、④第三者提供に
    関する方針、⑤本ポリシーの変更、⑥お問い合わせ窓口、⑦制定日
    （2026年7月28日）。**GA4タグ自体はまだ実装していない**ため、現時点の
    実態（未導入）と「導入予定」であることを区別して記載し、虚偽表示に
    ならないよう配慮した。既存3ページ（`index.html`/`about.html`/
    `404.html`）のフッターに「プライバシーポリシー」リンクを追加し
    （`css/style.css`に`.footer-links`/`.policy-date`クラスを追加）、
    `sitemap.xml`にも登録した（priority 0.3）。**検証**：ローカル静的
    サーバーで`privacy.html`/`index.html`/`about.html`/`404.html`/
    `css/style.css`/`sitemap.xml`が200で配信されることを確認済み。
    ブラウザ拡張（claude-in-chrome）は本セッションで未接続のため、実
    ブラウザでの目視確認は未実施のまま残る（以前の実装時と同じ制約）。
- **未決事項**：記事アーカイブ方針、英語版の要否、ドメイン取得・DNS設定・
  GitHub Pages有効化の実施（決定済み、付録D参照）、GA4タグの実際の実装
  （10月公開直前、Root CLAUDE.md第9章参照。実装後はprivacy.htmlの
  アクセス解析条項を「導入予定」から実態に合わせて更新する必要あり）
- **次のマイルストーン**：ドメイン`ginzawhiskers.com`の実際の取得と、
  付録Dの手順に沿ったGitHub Pages設定。またはヒーロー画像素材の取材・取得
- **最終更新日**：2026-07-28

---

# 付録A: コマンド

ビルド・lint・テストの仕組みはない。ローカルプレビューは任意の静的サーバーで行う：

```
python3 -m http.server 8000
```

`http://localhost:8000/index.html` を開く。

# 付録B: アーキテクチャ

- 4つのHTMLページ：`index.html`（アンカーリンクで区切られた1ページ構成：`#concept`,
  `#pillars`, `#journal`, `#contact`）、`about.html`、`404.html`、`privacy.html`。
  いずれも同じヘッダー・フッターのマークアップを持ち、同じ `css/style.css` と
  `js/main.js` を読み込む。テンプレート／インクルード機構が存在しないため、
  共有マークアップ（nav、footer）を変更する際は全HTMLファイルを手動で編集する
  必要がある（2026-07-28、フッターへの「プライバシーポリシー」リンク追加時に
  4ファイル全てを同期済み）。
- `css/style.css` は `:root` で定義されたCSSカスタムプロパティに基づく単一スタイルシート
  （色：近黒背景、オフホワイト文字、ゴールドアクセント／フォント：見出しにNoto Serif JP・
  Cormorant Garamond、本文にNoto Sans JP）。配色・タイポグラフィの変更は個別ルールではなく
  `:root` 変数を編集して行う。
- `js/main.js` が担うのは2つだけ：モバイル用ハンバーガーナビの開閉（`.nav-toggle` /
  `.main-nav.is-open`）と、フッターの `#year` への現在年の挿入。新しいインタラクティブな
  処理はすべてここに追加する（他にJSのエントリーポイントはない）。
- `images/` は現在`og-card.jpg`（OGP用ブランドカード）のみ——実際の写真は
  まだ追加されていない。2026-07-26にスコープを「ヒーロー画像1枚のみ」と
  確定済み（第9章・第10章参照）。実装仕様は次項。
- `favicon.svg` / `favicon.ico` / `apple-touch-icon.png`（リポジトリルート、
  2026-07-26追加）：「銀」の一文字をモチーフにしたブランドマーク。ブラウザの
  規約上サイトルート直下に置く必要があるため`images/`ではなくルートに配置。
  生成手順は`images/`ディレクトリではなく本セッションのスクラッチパッド上の
  Pythonスクリプト（Pillow使用）で行った一度きりの作業であり、リポジトリには
  成果物のみ含まれる。
- トップページの「ジャーナル」セクションはサイト内で記事をホストせず、外部の note.com
  アカウントへリンクしている。サイト内の記事アーカイブは現状存在しない。

## 付録B-1: ヒーロー画像 実装仕様（素材待ち、2026-07-26確定）

実素材（`images/hero.jpg` 想定）が届いた際に、以下の方針でそのまま実装できる
ようにしておく。実際のファイルが存在しない間はこのコード変更自体は行わない
（存在しない画像への参照を残さないため）。

- **配置**：`index.html` の `<section class="hero">` に背景画像として追加する
  （`<img>`タグではなくCSS `background-image`。テキストは引き続きHTML側に
  そのまま残し、画像はあくまで背景演出とする）。
- **可読性の担保**：現行の`.hero`は`radial-gradient(...) + var(--color-bg)`
  という近黒背景にゴールドの淡いグラデーションを重ねた構成（`css/style.css`
  159行目付近）。写真を敷く場合も、既存のこのグラデーション＋暗めの
  オーバーレイ（例：`linear-gradient(rgba(10,10,10,.55), rgba(10,10,10,.75))`
  程度）を画像の手前に重ね、白文字（オフホワイト）の可読性を必ず確保する。
  ブランドトーン（近黒・ゴールドアクセント）と衝突する明るい写真は避ける。
- **ファイル**：`images/hero.jpg`（またはOptimized WebPを用意できるなら
  `hero.webp` + `hero.jpg`フォールバック）。横長、最低1920×1080目安
  （`min-height: 86vh`のフルブリード表示に耐える解像度）。
- **alt/権利表記**：装飾的な背景画像のため`alt=""`扱い（CSS背景のため元々
  altは不要）。ただし写真の権利者・撮影者クレジットが必要な場合は、
  `.hero-inner`内に小さくクレジット表記を追加するかを素材到着時に判断する。
- **6本柱カードへの画像追加は今回のスコープ外**（2026-07-26決定）。テキストの
  みで継続し、必要になれば別途スコープを再検討する。

# 付録C: 既知のプレースホルダー

*TODOコメント形式のプレースホルダーは2026-07-26時点で全件解消済み。* `index.html`
にあった`TODO`コメント2箇所（`#journal`セクションのnote.com URL、`#contact`
セクションの連絡先`mailto:`）は、実際の値（`https://note.com/ginza_whiskers`、
`ginzashowaromanticclub@gmail.com`）への差し替えが完了した（第10章参照）。

**残っている暫定値（プレースホルダーではないが、ドメイン確定後に見直しが必要）**：
`index.html`/`about.html`のOGP `og:url`・`og:image`・canonicalタグ、および
`robots.txt`/`sitemap.xml`が、独自ドメイン未確定のため想定ドメイン
`https://ginzawhiskers.com/`をハードコードしていた（2026-07-26・2026-07-27追加、
第9章・第10章参照）。**2026-07-28にドメインを`ginzawhiskers.com`で正式確定
したため、これらの値は既に実際のドメインと一致しており更新不要**。今後
ドメインを変更する場合にのみ、これらのファイルの該当箇所を更新すること。

# 付録D: ドメイン/ホスティング設定手順（ランブック）

*2026-07-28にホスティング＝GitHub Pages、ドメイン＝`ginzawhiskers.com`で
決定（第10章参照）。以下は実際にドメインを取得した後に実施する手順。
本ランブック自体の作成はコード変更を伴わない。*

**前提**：リポジトリは`github.com/maron-ginza/GINZA_WHISKERS`にpush済み。

1. レジストラで`ginzawhiskers.com`を取得する（お名前.com、Cloudflare
   Registrar等。年額目安1,500〜2,000円）。
2. リポジトリ直下に`CNAME`ファイルを追加し、1行のみ`ginzawhiskers.com`と
   記述してコミットする。
3. レジストラ側のDNS設定で以下を追加する：
   - Apex（`ginzawhiskers.com`）→ GitHub Pages のAレコード4件
     （`185.199.108.153` / `.109.153` / `.110.153` / `.111.153`）
   - `www.ginzawhiskers.com` → CNAMEレコードで`maron-ginza.github.io`へ
4. GitHubリポジトリの Settings → Pages で以下を設定する：
   - Source：デプロイ元ブランチ（`main`、ルートディレクトリ）
   - Custom domain：`ginzawhiskers.com`を入力
   - DNSチェック通過後、「Enforce HTTPS」を有効化
5. 反映確認（DNS伝播には数分〜数時間かかる場合がある）：
   - `https://ginzawhiskers.com/` と `https://ginzawhiskers.com/about.html`
     がHTTPSで正しく表示されること
   - `https://ginzawhiskers.com/404.html`（存在しないパスへのアクセス）が
     カスタム404ページとして表示されること
   - `robots.txt`の`Sitemap:`行、`sitemap.xml`内のURL、各ページの
     `og:url`・`og:image`・canonicalタグが実際に到達可能であること
6. 完了後、第9章「インフラ」のチェックリスト項目にチェックを入れ、
   第10章に実施日と結果を記録する。

## 補足：現在のファイル配置について

2026-07-19の構造整理時点で、本プロジェクトの実装ファイル（`index.html`, `about.html`,
`css/`, `js/`, `images/`）は**リポジトリルート直下に残されており、このフォルダ
（`01-ginza-whiskers-brand-site/`）にはまだ移動していない**。これは見落としではなく、
ユーザーと合意した意図的な暫定方針である。実ファイルの本フォルダへの移行は、今後
あらためて提案・承認のうえで実施する（本憲章 第10章の未決事項を参照）。
